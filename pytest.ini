# Configuración por defecto para pytest
# -v: Modo verbose, muestra el nombre de cada test y su resultado.
# --tb=short: Traceback corto al fallar, muestra solo las líneas clave del error.
[pytest]
addopts = -v --tb=short 